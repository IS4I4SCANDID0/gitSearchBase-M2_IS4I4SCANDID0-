// import { surveyUser } from "./requests.js"

// await surveyUser(userName)

// export function renderProfile(users) {
//   const container = document.querySelector('.container')



//   users.forEach(user => {
    
//     const gitHubRepos = createCardRepos(user)
    
//     container.appendChild(gitHubRepos)
//   })
// }


// function createRepos(user) {
//   const userAvatar = document.createElement('img')
//   userAvatar.src = user.avatar_url
  
//   const userName = document.createElement('h2')
//   userName.innerText = user.login
  
//   const homePageBtn = document.createElement('button')
//   homePageBtn.innerText = 'Trocar de usuário'
// }